[project]
name = "blended-ner-framework"
version = "0.1.0"
description = "Chunk-based neuroscience NER pipeline with Grobid PDF parsing and per-chunk entity persistence"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.34.2",
]
